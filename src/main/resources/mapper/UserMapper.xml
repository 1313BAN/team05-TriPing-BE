<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="com.ssafy.enjoytrip.model.mapper.UserRepository">
 
    <insert id="insertUser">
        INSERT INTO user (username, user_email, user_password)
        VALUES (#{username}, #{userEmail}, #{userPassword})
    </insert>
    
    <select id="login" resultType="com.ssafy.enjoytrip.model.dto.UserDTO">
        SELECT *
        FROM user
        WHERE user_email = #{userEmail}
        AND user_password = #{userPassword}
    </select>
    
    
    <!-- ⭐ 추가: 회원정보 수정 -->
    <update id="updateUser">
        UPDATE user
        SET username = #{username},
            user_email = #{userEmail},
            user_password = #{userPassword}
        WHERE id = #{id}
    </update>

    <!-- ⭐ 추가: 회원 id로 회원정보 조회 -->
    <select id="selectUserById" resultType="com.ssafy.enjoytrip.model.dto.UserDTO">
        SELECT *
        FROM user
        WHERE id = #{id}
    </select>
    
 </mapper>